# cat=development//; type=string; label= Redirect to: List of email addresses where to redirect all messages for "development" context. Notice, it can also be defined in $GLOBALS['TYPO3_CONF_VARS']['MAIL']['development_redirect_to'] = 'email@example.com' which will take the precedence if configured so..
development_redirect_to =

# cat=production//; type=string; label= Redirect to: List of email addresses where to redirect all messages for "production" context. Notice, it can also be defined in $GLOBALS['TYPO3_CONF_VARS']['MAIL']['production_redirect_to'] = 'email@example.com' which will take the precedence if configured so..
production_redirect_to =

# cat=testing//; type=string; label= Redirect to: List of email addresses where to redirect all messages for "testing" context. Notice, it can also be defined in $GLOBALS['TYPO3_CONF_VARS']['MAIL']['testing_redirect_to'] = 'email@example.com' which will take the precedence  if configured so..
testing_redirect_to =

